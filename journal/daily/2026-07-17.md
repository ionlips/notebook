# Friday, July 17, 2026

Finished setting up my configuration files. Created a new branch (ide/vs-code)
that considers VS Code my default IDE (as opposed to Neovim via LazyVim).[^1]

Having created a footnote, I can see that Neovim automatically wraps my text
and indents it by four spaces. In general, lists and numbered lists will follow
the following convention:[^2] [^3]

```text
-   Note how I include three spaces after the hyphen. This is so that text,
    which will wrap at four spaces, is nicely aligned.
    -   Nested lists will also follow the same convention.
1.  Numbered lists take two spaces (due to the addition of a period) for the
    same reason.
    1.  Also, we use "lazy" numbering.
    1.  Looks quite nice, no?
```

Joseph made a programme that extracts project information from `myquota` on
MeluXina using Streamlit. I made a change to it where it compares the project
code to our own internal `Migrated Projects.xlsx` file (statically stored
within the repository, as of now) and inputs the project's PI's details into
the dashboard.

Created a new GitHub account.

[^1]: TODO: Update `Makefile` to properly symlink files under VS Code's
    directory depending on the system (i.e., GNU/Linux or macOS).
[^2]: Unfortunately, Neovim doesn't seem to automatically indent by four spaces
    when it comes to lists.
[^3]: However, it does indent by four spaces when it comes to numbered lists.
