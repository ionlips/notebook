# Wednesday, July 22, 2026

Spent some time trying to understand why nvim-treesitter would not install
properly on qllm. I have installed Tree-sitter's CLI version 0.26.9 which
requires glibc version 2.39, or greater, at compile-time. Thus, according to
Claude, the best course of action is to compile it from source myself. I did
the following:

```shell
# Install `rustup` for Rust. Note that I chose not to modify my `.rc` files.
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# Install Tree-sitter's CLI.
~/.cargo/bin/cargo install tree-sitter-cli \
    --locked \
    --no-default-features \
    --version 0.26.11
```

Switch the new binary for the old.

Had a lot of trouble getting Neovim configured on my Ubuntu 26.04 system.
mason.nvim required npm in order to install a couple of formatters and linters
(markdownlint-cli2, Ruff, etc.) but for some reason, my `.zshrc` file would not
source properly. I closed Ghostty, killed the tmux server (multiple times), and
quit Neovim until it just worked... for some reason.

TODO: When I have time, I really need to go through the external plugins
LazyVim installed for me so that I can reduce the bloat. It just seems like
there are too many plugins and features I simply don't need.
